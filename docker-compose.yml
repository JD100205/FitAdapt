services:
  db:
    image: mysql:8.0
    environment:
      MYSQL_ROOT_PASSWORD: root
      MYSQL_DATABASE: fitadapt_db
    ports:
      - "3307:3306"
    volumes:
      - ./fitadapt.sql:/docker-entrypoint-initdb.d/fitadapt.sql